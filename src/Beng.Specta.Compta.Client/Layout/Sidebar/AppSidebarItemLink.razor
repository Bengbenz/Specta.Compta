@typeparam TMenuItem

@if(!IsMinimized)
{
    @MenuItemTemplate(TItem)
}
else
{
    <VaTooltip Message="@Tooltip" Position="@PositionType.Right">
        @MenuItemTemplate(TItem)
    </VaTooltip>
}

@code{

    [Parameter] public TMenuItem TItem { get; set; } = default!;
    [Parameter] public RenderFragment<TMenuItem> MenuItemTemplate { get; set; } = null!;
    [Parameter] public string? Tooltip { get; set; }
    [Parameter] public bool IsMinimized { get; set; }
}